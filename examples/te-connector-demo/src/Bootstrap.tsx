'use client';

import { SitecorePagePropsContext } from '@sitecore-content-sdk/nextjs';

type BootstrapProps = {
  siteName: string;
  isPreviewMode: boolean;
};

export default function Bootstrap({ siteName, isPreviewMode }: BootstrapProps) {
  return null;
}
